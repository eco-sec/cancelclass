<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
	<Dialog id="CreateOnBehalfDialog" title="Cancel Class On Behalf" draggable="true" resizable="true">
		<VBox class="sapUiSmallMargin">
			<Label text="Select Subordinate:"/>
			<Select id="subordinateSelect" width="100%" items="{subordinateModel>/EmployeeList}" change="onSubordinateChange">
				<core:Item key="{subordinateModel>EmpPernr}" text="{= ${subordinateModel>EmpPernr} + ' - ' + ${subordinateModel>EmpEnglishName} }"/>
			</Select>
			<Label text="Select Class:" class="sapUiSmallMarginTop"/>
			<Select id="classSelect" width="100%" items="{classModel>/ClassList}">
				<core:Item key="{classModel>CLASS_ID}" text="{classModel>CLASS_TITLE}"/>
			</Select>
		</VBox>
		<beginButton>
			<Button text="Proceed" type="Emphasized" press="onConfirmCancelOnBehalf"/>
		</beginButton>
		<endButton>
			<Button text="Cancel" press="onCancelOnBehalf"/>
		</endButton>
	</Dialog>
</core:FragmentDefinition>